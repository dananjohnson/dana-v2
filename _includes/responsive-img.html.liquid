{% comment %}
Usage: {% include picture.html.liquid src="img.png" alt="Alt text" %}
{% endcomment %}

{% asset '{{ include.src }}'
    srcset:width=300
    srcset:width=600
    srcset:width="1200 2x"
    sizes="(min-width: 40em) 600px, calc(100vw - 1em)"
    alt="{{ include.alt }}"
      %}
